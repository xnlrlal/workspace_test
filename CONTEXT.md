# 프로젝트 컨텍스트 (클로드 코드 인계 노트)

> 이 문서는 claude.ai 채팅에서 이 프로젝트를 설계/구현한 내용을 요약한 것입니다.
> 클로드 코드에서 "CONTEXT.md 읽고 이어서 진행해줘"라고 하면 아래 맥락을 기반으로
> 바로 이어서 작업할 수 있습니다.

## 무엇을 만들고 있나

과거 주가 데이터를 기반으로 국내+해외 주식의 단기 추세를 예측/시각화하는
Flask 웹 대시보드 ("시그널"). 사용자(성호)는 한국의 초보 투자자로, 토스/카카오페이
증권 계좌를 Core-Satellite 전략으로 운용 중이며, KEPCO 등 개별 종목과 배당 성장
ETF에 관심이 많음. Open DART API 기반 기관투자자 추적 툴도 별도로 진행 중.

## 왜 이런 선택을 했는가

- **예측 모델로 ARIMA를 선택한 이유**: 사용자가 "잘 모르겠음, 추천해줘"라고 답해서
  직접 추천함. Prophet(설치 무거움, 컴파일 필요)과 LSTM(과적합 위험, 데이터 요구량
  많음) 대신, 가볍고 해석 가능하며 신뢰구간을 기본 제공하는 statsmodels ARIMA를
  선택. AIC 기준으로 (p,d,q) 그리드서치 자동 선택.
- **국내/해외 통합**: 사용자가 "국내+해외 둘 다"를 선택함 → 6자리 숫자 종목코드면
  FinanceDataReader(KRX), 아니면 yfinance(Yahoo Finance)로 자동 분기.
- **디자인 톤**: 한국 주식 시장 관례(상승=빨강, 하락=파랑)를 따름. 다크 트레이딩
  터미널 느낌 + Pretendard(한글) / JetBrains Mono(숫자) 조합. "예측 팬"(forecast
  fan)이 시그니처 요소 — 마지막 실측 종가에서 이어지는 80% 신뢰구간 음영.
- **투자 자문 아님을 명시**: 대시보드 하단에 디스클레이머 고정 배치. ARIMA는 뉴스/
  수급/실적 등을 반영하지 못하므로 참고용임을 강조.

## 현재 상태

전체 기능 구현 완료, Flask 테스트 클라이언트로 API 파이프라인 검증 완료
(지표 계산 → ARIMA 예측 → JSON 직렬화). 스크린샷으로 레이아웃도 확인함.

**단, 실제 KRX/Yahoo Finance 네트워크 호출은 검증 환경(샌드박스) 네트워크 제한 때문에
합성(랜덤) 데이터로만 테스트했음.** 클로드 코드 환경(로컬)에서 `python app.py` 실행 후
`http://127.0.0.1:5000`에서 실제 데이터로 첫 검증이 필요함.

파일 구조는 README.md 참고.

## 다음으로 고려해볼 만한 것 (README에도 있음)

1. **워치리스트 저장**: 지금은 사이드바에 종목 6개가 하드코딩되어 있음. SQLite나
   브라우저 저장 방식으로 사용자가 관심 종목을 추가/삭제할 수 있게 확장.
2. **모델 비교**: ARIMA 외에 Prophet이나 간단한 이동평균 회귀를 옵션으로 추가해
   토글 비교.
3. **백테스트 페이지**: 과거 시점 기준으로 예측이 실제로 얼마나 맞았는지 검증하는
   화면. 모델 신뢰도를 사용자가 스스로 가늠할 수 있게 함.
4. **Open DART 프로젝트와의 연계 가능성**: 사용자가 별도로 진행 중인 기관투자자
   추적 툴(Flask 백엔드)과 데이터 소스나 UI 톤을 공유할 수 있는지 검토해볼 만함
   (단, 먼저 사용자에게 의향을 확인할 것).

## 알아두면 좋은 것

- `services/data_fetcher.py`의 `_col_to_list` 관련 버그를 하나 수정한 이력이 있음:
  pandas Series에 `.where(cond, None)`을 쓰면 float64 dtype에서 None이 다시 NaN으로
  캐스팅되어 JSON에 `NaN` 리터럴(비표준, 브라우저 `JSON.parse` 실패 유발)이 섞이는
  문제가 있었음. 리스트 컴프리헨션으로 명시적 변환하도록 수정함
  (`app.py`의 `_col_to_list` 참고). 비슷한 패턴을 새로 짤 때 주의할 것.
